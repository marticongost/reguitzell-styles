A SASS framework, providing convenient utilities for common concerns.

## Installation

```sh
npm install --save-dev @reguitzell/styles
```

## Usage

Sample usage:

```scss
@use "@reguitzell/styles" as rz;

@include rz.vpadding(xl);
```
